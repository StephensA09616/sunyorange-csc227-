/*
*Exersie 2
*/


function quotient(x, y) {
    return Math.floor (x / y);
}
 let x = 10;
 let y = 2;

 console.log (quotient(x, y));

 /*
 *Exersie 3
 */


function sumFirstNIntergers(N) {
   return (N*(N+1)/2);
}

let N = 3;
console.log (sumFirstNIntergers(N));


/*
*Exersie 5
*/

function areaOfTriangle(h, b) {
    return (1/2 * b * h);
}

let b = 4;
let h = 4;

console.log (areaOfTriangle(h, b));